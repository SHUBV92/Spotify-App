import React from 'react'


const Songs = ({ songs }) => {
    return (
        <div>
            <center><h1>Songs List</h1></center>
            {songs.map((song) => (
                
                
                
            ))}
        </div>
    )
}

export default Songs 